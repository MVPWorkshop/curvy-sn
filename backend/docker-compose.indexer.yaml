services:

  indexer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: curvy_indexer
    ports:
      - "4000:4000"
    networks:
      - curvy_network
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
